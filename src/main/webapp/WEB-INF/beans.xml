<?xml version="1.0" encoding="UTF-8"?>
<!--
  beans.xml - CDI (Contexts and Dependency Injection) configuration
  
  CDI is Jakarta EE's dependency injection framework.
  Similar to .NET's built-in DI container.
  
  This file enables CDI for the application.
  Must be in META-INF/ for JAR or WEB-INF/ for WAR.
-->
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
                           https://jakarta.ee/xml/ns/jakartaee/beans_3_0.xsd"
       version="3.0"
       bean-discovery-mode="all">
    <!--
      bean-discovery-mode="all": 
      Scan and register all classes in the archive as potential beans.
      Alternative: "annotated" - only classes with CDI annotations are beans.
    -->
</beans>
